// A simple grammar definition for string literals and concatenation
start: expression
expression: string_literal
          | expression "+" expression -> concat
string_literal: ESCAPED_STRING
%import common.WS_INLINE
%ignore WS_INLINE  // Ignore spaces outside of string literals
